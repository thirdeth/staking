import { FC } from 'react';
import cn from 'clsx';

import s from './styles.module.scss';

export interface {{ properCase name }}Props {
  className?: string;
}

export const {{ properCase name }}: FC<{{ properCase name }}Props> = ({ className }) => {
  return (
    <div className={cn(s.{{ camelCase name }}, className)}>
      Hello World!
    </div>
  );
};
